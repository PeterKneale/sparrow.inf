apiVersion: v1
kind: Pod
metadata:
  name: sparrow-api
spec:
  containers:
    - name: sparrow-api
      image: gcr.io/simplicate-sparrow-project/api
      livenessProbe:
          httpGet:
            path: /api/users
            port: 80
      readinessProbe: 
          httpGet:
            path: /api/users
            port: 80
      env:
        - name: DB_PORT_6379_TCP_ADDR
          value: 10.12.2.3
        - name: DB_PORT_6379_TCP_PORT
          value: "5432"
        - name: DB_DATABASE
          value: sparrow
        - name: DB_USERNAME
          value: sparrow
        - name: DB_PASSWORD
          value: password
      ports:
        - containerPort: 80
          name: sparrow-api
