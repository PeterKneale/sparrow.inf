apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ing
  labels:
    component: ing
spec:
  rules:
  - host: webapitester.com
    http:
      paths:
      - backend:
          serviceName: web-svc
          servicePort: 80
  - host: api.webapitester.com
    http:
      paths:
      - backend:
          serviceName: api-svc
          servicePort: 80
